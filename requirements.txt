# NFT Truth Layer - Python Dependencies

# NOTE: there is no standalone pip-installable "genlayer" package for the
# contract-side SDK (gl.Contract, TreeMap, u256, etc.) - that library only
# exists inside the GenVM execution environment (Docker localnet / Studio /
# testnet), resolved via the "# { Depends: py-genlayer:... }" header in the
# contract file itself. Do NOT `pip install genlayer` - the package under
# that name on PyPI is an unrelated placeholder, not the real SDK.

# GenLayer test framework (gltest) - deploys/runs contracts against a real
# GenVM sandbox (localnet or studionet) with mocked LLM responses, real
# TreeMap/eq_principle semantics, etc. This is the only package you need
# to actually run contracts/nft_verifier.py locally.
genlayer-test

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Utilities
requests>=2.31.0
python-dotenv>=1.0.0
pyyaml>=6.0.1

# Development
black>=23.0.0
flake8>=6.0.0
